apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm-chart.componentName" (dict "root" . "component" "backend") }}-secret
  labels:
    {{- include "helm-chart.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
type: Opaque
stringData:
  MYSQL_PASSWORD: {{ .Values.backend.env.MYSQL_PASSWORD | quote }}
  
